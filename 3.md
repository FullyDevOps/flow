## 1) Mind-map template — **FSVP Binder per SKU (repeat for all 10 SKUs)** 🧾

Paste into `docs/fsqp-fsvp-per-sku-template.md`:

```mermaid
mindmap
  root((FSVP Binder - SKU TEMPLATE))
    00_SKU_Profile
      SKU_ID_and_Name
      Product_Description(powder, low-moisture)
      Intended_Use(food ingredient, beverage mix, etc.)
      Packaging(8oz/16oz; pouch/jar)
      Shelf_Life_Assumption
    01_Hazard_Analysis
      Biological(Salmonella, etc.)
      Chemical(heavy metals, pesticides)
      Physical(foreign matter, metal)
      Allergen_CrossContact(statement + controls)
      Preventive_Controls_Summary(what supplier does)
    02_Supplier_Evaluation
      Supplier_Profile(manufacturer details)
      Food_Safety_System(GMP/SSOP evidence)
      Lab_Capability(ISO17025 scope)
      Performance_History(complaints, fails)
      Approval_Decision_Memo
    03_Verification_Activities
      Option_A_Audit(annual/biannual)
      Option_B_Testing(per-lot + periodic)
      Option_C_Records_Review(COAs + logs)
      Frequency_and_Rationale
      Acceptance_Criteria(mesh/moisture/aw/micro/metals/pesticides)
    04_Corrective_Actions
      When_to_Trigger(fail COA, complaint spike)
      Containment(hold/stop ship)
      Investigation(root cause)
      Disposition(rework/reject)
      CAPA_tracking
    05_Reevaluation
      Scheduled_Reeval(every 3 years)
      Event_Reeval(supplier change, spec change)
      Document_Change_Log
    06_Records
      COAs(per lot)
      Shipment_Linkage(lot->shipment->entry)
      Retain_Samples(log)
      Training(roles, signoffs)
      Retention_Rules
```

---

## 2) ERD — **Carton/Pallet optimization + Landed Cost tracking** 💰📦

Paste into `docs/erd-landed-cost.md`:

```mermaid
erDiagram
  SUPPLIER ||--o{ PURCHASE_ORDER : receives
  PURCHASE_ORDER ||--o{ PO_LINE : contains
  SKU_PACK ||--o{ PO_LINE : ordered_item

  PURCHASE_ORDER ||--o{ SHIPMENT : fulfills
  SHIPMENT ||--o{ SHIPMENT_LINE : contains
  FINISHED_LOT ||--o{ SHIPMENT_LINE : shipped_lot
  SKU_PACK ||--o{ SHIPMENT_LINE : shipped_as

  SHIPMENT ||--o{ COST_ITEM : has
  COST_TYPE ||--o{ COST_ITEM : classifies
  FX_RATE ||--o{ COST_ITEM : converted_by

  SHIPMENT ||--o{ PALLET_PLAN : plans
  PALLET_PLAN ||--o{ CARTON_PLAN : includes
  CARTON_PLAN ||--o{ PACK_PATTERN : uses

  SHIPMENT ||--o{ DUTY_TAX : incurs

  SHIPMENT ||--|| RECEIPT : results_in
  RECEIPT ||--o{ RECEIPT_LINE : details
  SHIPMENT_LINE ||--o{ RECEIPT_LINE : reconciles

  RECEIPT ||--o{ COST_ALLOCATION : allocates
  SKU_PACK ||--o{ COST_ALLOCATION : allocated_to

  SUPPLIER {
    string supplier_id PK
    string supplier_name
    string country
    boolean organic_certified
  }

  PURCHASE_ORDER {
    string po_id PK
    string supplier_id FK
    date po_date
    string incoterm "FOB|CIF|DDP|EXW"
    string currency
    string payment_terms
    string status "draft|issued|closed"
  }

  PO_LINE {
    string po_line_id PK
    string po_id FK
    string sku_pack_id FK
    int qty_units
    float unit_price
  }

  SHIPMENT {
    string shipment_id PK
    string po_id FK
    string mode "ocean|air"
    string lcl_fcl "LCL|FCL|NA"
    string origin_port
    string us_port
    date etd
    date eta
    float total_gross_kg
    float total_cbm
  }

  SHIPMENT_LINE {
    string shipment_line_id PK
    string shipment_id FK
    string finished_lot_id FK
    string sku_pack_id FK
    int units
    int cases
    int pallets
  }

  COST_TYPE {
    string cost_type_id PK
    string name "freight|insurance|broker|duty|port_fees|drayage|3pl_inbound|prep|storage"
    string category "origin|in_transit|destination|post_import"
  }

  COST_ITEM {
    string cost_item_id PK
    string shipment_id FK
    string cost_type_id FK
    float amount
    string currency
    float fx_to_usd
    float amount_usd
    string note
  }

  FX_RATE {
    string fx_id PK
    string pair "INRUSD|USDUSD"
    date fx_date
    float rate
    string source
  }

  DUTY_TAX {
    string duty_tax_id PK
    string shipment_id FK
    string tax_type "duty|mpf|hmf|other"
    float amount_usd
    string broker_ref
  }

  PALLET_PLAN {
    string pallet_plan_id PK
    string shipment_id FK
    string pallet_type "plastic|wood"
    boolean ispm15_required
    int pallet_count
  }

  CARTON_PLAN {
    string carton_plan_id PK
    string pallet_plan_id FK
    string sku_pack_id FK
    int cartons
    int units_per_carton
    float carton_L_cm
    float carton_W_cm
    float carton_H_cm
    float carton_gross_kg
  }

  PACK_PATTERN {
    string pattern_id PK
    string name "A1|B2"
    int cartons_per_layer
    int layers
    string notes
  }

  RECEIPT {
    string receipt_id PK
    string shipment_id FK
    date received_date
    string location "IN_warehouse|3pl|prep_center"
    string status "quarantine|released|discrepancy"
  }

  RECEIPT_LINE {
    string receipt_line_id PK
    string receipt_id FK
    string shipment_line_id FK
    int units_received
    int cases_received
    int units_damaged
  }

  COST_ALLOCATION {
    string alloc_id PK
    string receipt_id FK
    string sku_pack_id FK
    float allocated_cost_usd
    float landed_cost_per_unit_usd
    string basis "units|weight|volume|value"
  }
```

---

## 3) Flowchart — **Landed cost calculation (clean, auditable)** 🧮

Paste into `docs/flows-landed-cost.md`:

```mermaid
flowchart TD
  A["Collect all cost inputs"] --> B["Product cost from PO"]
  A --> C["Freight and insurance"]
  A --> D["Origin and destination fees"]
  A --> E["Broker and documentation fees"]
  A --> F["Duties and MPF and HMF"]
  A --> G["3PL and prep inbound costs"]

  B --> H["Convert all costs to USD using FX rate"]
  C --> H
  D --> H
  E --> H
  F --> H
  G --> H

  H --> I{"Choose allocation basis"}
  I -->|Units| J["Allocate by unit count per SKU pack"]
  I -->|Weight| K["Allocate by weight per SKU pack"]
  I -->|Volume| L["Allocate by cubic volume per SKU pack"]
  I -->|Value| M["Allocate by product value per SKU pack"]

  J --> N["Compute landed cost per unit"]
  K --> N
  L --> N
  M --> N

  N --> P["Set price model"]
  P --> Q["Add Amazon fees and margin and promo buffer"]
  Q --> R["Set MSRP for store and Amazon"]

```

---

## 4) Flowchart — **Cartonization + Palletization planner (pouch vs jar)** 📦

Paste into `docs/flows-cartonization.md`:

```mermaid
flowchart TD
  A[Choose SKU_PACK] --> B{Format}
  B -->|Pouch| P1[Select pouch case pack]
  B -->|Jar| J1[Select jar case pack]

  P1 --> P2[Target: stable cube + low crush risk]
  J1 --> J2[Target: breakage control + dividers]

  P2 --> C[Compute carton dims + gross kg]
  J2 --> C

  C --> D{Meets carrier limits?}
  D -->|Yes| E[Select pallet type (plastic preferred)]
  D -->|No| F[Adjust units per carton / carton spec]
  F --> C

  E --> G[Pick pattern: cartons per layer + layers]
  G --> H[Compute pallet height + gross kg + CBM]
  H --> I{Within safe handling?}
  I -->|Yes| J[Lock carton label + pallet label templates]
  I -->|No| K[Reduce layers / change carton]
  K --> G
```

---

## 5) Sequence diagram — **Label change management (prevent “wrong label” disasters)** 🏷️

Paste into `docs/seq-label-change.md`:

```mermaid
sequenceDiagram
  autonumber
  participant You as You (Brand Owner)
  participant QA as Friend QA (India)
  participant Printer as Printer/Packaging Supplier
  participant Prod as Production

  You->>You: Create new Label Version (rev++)
  You->>QA: Send updated artwork + change summary + effective date
  QA->>QA: Regulatory checklist + claims check + barcode check
  QA-->>You: Approval/notes
  You->>Printer: Issue "Label Release" (approved PDF + rev)
  Printer-->>You: Proof for signoff
  You->>Printer: Final proof approval
  QA->>Prod: Line clearance + remove old labels
  Prod->>Prod: Start run using new rev
  QA->>QA: First-article label verification (photo + sign)
  QA-->>You: Evidence packet (photos + BMR refs)
```

---

## 6) Mind-map — **Templates pack (what to store in repo for audit readiness)** 📁

Paste into `docs/templates-mindmap.md`:

```mermaid
mindmap
  root((Templates Pack))
    Batch_Manufacturing_Record(BMR)
      Receiving_Log
      Dryer_Log(time/temp)
      aw_moisture_checks
      Sieve_Checks
      Metal_Detector_Checks
      Packaging_Weights
      Deviation_Log
      QA_Release_Form
    Label_Release
      Regulatory_Checklist
      Claims_Checklist
      Barcode_Checklist
      Proof_Approval_Signoff
      Effective_Date
    Receiving_QC(US)
      Quarantine_Tag
      Count_Verification
      Damage_Report
      Lot_BestBy_Verification
      Photo_Checklist
      Release_To_Inventory
    Complaint_Form
      Severity
      Lot_ASIN_UPC
      Photos
      Outcome
      CAPA_Link
    Recall_Plan
      Contact_Tree
      Mock_Recall_Steps
      Lot_Scope_Logic
      Retrieval_Logs
```

---

If you want the next “continue” set, I can generate:

* **ERD for Amazon performance + returns + review mining** (ASIN → defects → CAPA)
* **Mind-map for a “Single Master Compliance Dashboard”** (KPIs: COA pass rate, holds, defect rate, FEFO accuracy)
